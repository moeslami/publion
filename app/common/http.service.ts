import {AppConstants} from '../common/constants';
import {HTTP_PROVIDERS} from 'angular2/http';

export class HttpService{

    get(resource: string, requiresAuth: boolean){
        if(requiresAuth){
            
        }
    }
    
    post(resource: string, requiresAuth:boolean, data:any){
        
    }
    
    put(){
        
    }
    
    delete(){
        
    }
}